"""pdfx_lib — building blocks for the Markdown→PDF ebook generator.

cover         : AI cover generation (Gemini) + API-key discovery
mermaid       : render ```mermaid fenced blocks to SVG (best-effort)
markdown_render: chapter rendering, inline-SVG protection, badges, heading ids
toc_index     : table of contents (depth-aware), A-Z index, part dividers
styles        : print CSS (cover, TOC, chapters, parts, index)
"""
